# Local secrets / scratch — never commit
*.env
*.local
.DS_Store

# Real account map (copy of accounts.example.md) — never commit
orchestration/mapping/accounts.md
**/orchestration/mapping/accounts.md

# Minimal-storage policy — never commit heavy/raw uploads or raw workspace
# transcripts. Approved Markdown knowledge lives under knowledge/ (committable).
uploads/
**/uploads/
/transcripts/

# Claude credentials — never commit
**/.credentials.json
.claude/.credentials.json
.claude/
.claude.json

# OpenViking semantic memory — config, data, and API keys NEVER committed
.openviking/
**/.openviking/
ov.conf
**/ov.conf
