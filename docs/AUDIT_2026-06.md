# System Audit — June 2026 (Phases 1–2)

## Phase 1 — Model update

| Item | Value |
| --- | --- |
| Claude Code (VPS, `wayan`) | 2.1.170 |
| Old model | *(unset → account default, resolved to `claude-opus-4-8[1m]`)* |
| New model | **`claude-fable-5`** — availability **verified empirically** (`claude -p --model claude-fable-5` answers and self-identifies) |
| Where it lives | `~wayan/.claude/settings.json` → `"model"` field (backup: `settings.json.bak.model`) |
| Verified | Jupiter **and** Uran `claude -p` both answer as `claude-fable-5`; memory_recall works on the new model |

**How future users change the model:**
1. Interactive: run `claude` as `wayan` → `/model <name>` (saves as default), or
2. Edit `~wayan/.claude/settings.json` and set `"model": "<id>"`.
3. The gateway passes no `--model`, so agents always inherit this default — no
   code change or service restart needed (each message spawns a fresh `claude -p`).
4. **Never hardcode a model that isn't verified**: test first with
   `claude -p --model <id> "Reply OK"`.

## Phase 2 — Audit table

| Area | Result | Evidence |
| --- | --- | --- |
| VPS OS | ✅ PASS | Ubuntu 22.04.5, up 12 weeks |
| RAM / swap | ⚠️ **WARN** | 951 MB total, 348 MB avail, **636 MB swap used** — chronic pressure; recommend 2 GB plan |
| Disk | ✅ PASS | 14G/30G (50%) |
| Docker / OpenViking | ✅ PASS | engine 29.5.3; container healthy `v0.3.24`, `auth_mode=api_key`, bound `127.0.0.1:1933` only |
| systemd / Jupiter / Uran | ✅ PASS | both active; 0 failed units |
| Telegram connectivity | ✅ PASS | connect events in last 24 h |
| Groq | ✅ PASS | 4 successful transcriptions in last 24 h (live voice test) |
| OpenAI / memory backend | ✅ PASS | end-to-end `memory_recall` returns stored facts (tenant `wayan/taras`) |
| Gateway: text / voice / PDF | ✅ PASS | live-tested (voice + PDF on 2026-06-10; transcripts written, raw deleted) |
| Gateway: image | ✅ PASS (code+tests) | classifier handles photos; covered by unit tests |
| Gateway: video | ⚠️ **WARN** | classifier supports `video`/`video_note`/video-MIME, all paths reply in code review, but **no immediate ack** and no live video event in 7 days of logs — silent-video reports likely predate the classifier; needs Phase 3 hardening + operator retest |
| Gateway: links / large-file flow | ✅ PASS (code+tests) | size/disk gates, PROCESS FILE/LINK, SSRF blocks — unit-tested |
| **Parallel tasks / queue** | ❌ **FAIL** | **No queue exists** — single sequential loop (`app.py:837`), no received-ack; rapid tasks feel like unresponsiveness. Root cause of UX bug #1. → Phase 3 |
| Storage policy (cleanup/transcripts) | ✅ PASS | `uploads/tmp` empty on both agents; transcripts retained (3+2) |
| Markdown memory / orchestration | ✅ PASS | 6 skills + 5 orchestration dirs + Day 2 section in both workspaces |
| Skills routing + chaining | ✅ PASS | onchain-alpha, server-ops verified live; 3-skill chain verified earlier |
| Memory store/recall/shared | ✅ PASS | J→U and U→J verified; re-verified post-model-change |
| No secrets in memory | ✅ PASS | no key-like strings in `~/.openviking/data` |
| Permissions (role profiles) | ✅ PASS | deployed == repo templates (sha match); memory MCP tools allowed; dangerous ops gated; no-self-edit deny active; headless runs with 0 prompts |
| Git safety | ✅ PASS | no env/keys/ov.conf/uploads/transcripts tracked; `.gitignore` covers all; no real server IP in repo |
| Install reproducibility | ✅ PASS | installer deploys gateway+skills+orchestration+settings no-clobber; 72/72 tests green |

### FAIL/WARN dispositions
- **Queue (FAIL):** fix commissioned as Phase 3 (per-chat queue + ack + /queue,
  /cancel + timeout). Code change — design reviewed before merge.
- **Video ack (WARN):** Phase 3 adds an immediate `🎥` ack + regression tests;
  operator retest after deploy.
- **RAM (WARN):** operational recommendation — upgrade to ≥2 GB; not blocking.

*Note:* spec name `CLAUDE_TASK_TIMEOUT_SEC` — implemented today as
`CLAUDE_TIMEOUT` (default 300 s) in env; Phase 3 keeps the existing name and
adds per-task timeout messaging.

## Phase 4 addendum — Visual video analysis (2026-06-11)

The two video-related findings above are now **resolved and live-verified**:

| Area | Result | Evidence |
| --- | --- | --- |
| Gateway: video (was WARN) | ✅ **PASS (live)** | 2026-06-11 06:51 UTC production test: `telegram_update_received` → `media_detected kind=video` → `media_ack_sent … visual=True` → `media_download_completed` (2.8 MB) → Groq transcript → `frames: extracted 5 frame(s)` → `video visual analysis prepared: 5 frame(s), audio=yes` → `reply sent (1984 chars)` → `frames: removed frames dir` + analysis transcript written. Operator confirmed reply quality. |
| Queue (was FAIL) | ✅ PASS | Phase 3 queue shipped; live video ran through it (`pos=1` ack), worker thread active on both agents |
| **Visual analysis** (new) | ✅ PASS | gateway `1.1.0a11` on Jupiter **and** Uran (code identical, md5-verified); `VIDEO_VISUAL_ANALYSIS=true` confirmed in both env files **and** live process environments; ffmpeg 4.4.2 detected at `/usr/bin/ffmpeg` |
| Storage policy after video | ✅ PASS | `uploads/tmp/` and `uploads/videos/` both empty post-run; only Markdown transcripts persist |
| OpenViking exposure | ✅ PASS | container still published `127.0.0.1:1933` only |
| Services | ✅ PASS | wayan-jupiter + wayan-uran active, NRestarts=0 since env rollout |
| Secrets | ✅ PASS | journald shows no tokens/keys; repo secret-scan of synced gateway code clean |

Remaining open WARNs: **RAM** (951 MB, chronic swap — recommend ≥2 GB plan).

## Phase 5 addendum — Host hardening, port 8080 (2026-06-11)

The `wayan_pirat_bot` exposure WARN is **resolved** (ROADMAP M6 phase 1):

| Item | Finding / action |
| --- | --- |
| Who needed inbound 8080? | **Nobody.** Telegram = long polling; `HELIUS_INGEST_ENABLED=false` (1088 webhook POSTs/week were ACK'd and discarded); `/health` probed only by the localhost `wayan-healthcheck.timer` |
| Risk found | `/webhook/helius` had no auth (`HELIUS_WEBHOOK_AUTH` empty) on a world-reachable port; live scanner traffic (`/.env`, `/wp-config.php`, `/backup.sql` probes) |
| Fix | `WEBHOOK_HOST=127.0.0.1` in its `.env` (timestamped backup kept on host) + service restart + `ufw delete allow 8080/tcp` (v4+v6) |
| Verified | uvicorn bound `127.0.0.1:8080`; `/health` 200 locally; `healthcheck.sh` rc=0; polling live (`@Wayan_pirate_bot`); `ss -tlnp` public listeners now only 22/sshd and 80/nginx (80 not in ufw allow list); ufw allows 22 only; OpenViking still `127.0.0.1:1933`; wayan-jupiter/uran untouched |
| Remaining (M6 phases 2–3) | de-root `wayan-bot` (dedicated user + systemd hardening — plan accepted, see `M6_WAYAN_BOT_ROOTLESS_PLAN.md`); optionally delete the stale Helius webhook registration |
| **Env permissions fixed (2026-06-11)** | `/opt/wayan_pirat_bot/.env` + 3 historical `.env` backups: 644 (world-readable, live keys) → **600**, ownership `root:root` unchanged, contents untouched, no restart needed; verified service `active` + healthcheck rc=0 after |
